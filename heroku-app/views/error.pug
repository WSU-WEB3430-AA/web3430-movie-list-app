extends layout.pug
block content
  div.mx-5
    div(class="row mt-3 mb-4 pb-3 border-bottom border-3 border-primary")
      div(class="col-10 d-grid")
        h3 #{ err.message || 'Something wend wrong!' }
      div(class="col-2 d-grid")
        p(class="mt-3 text-end text-danger") Code #{err.status}
    div
      pre #{err.stack}